//Advanced bash commands
/*
1.ls = you can also use ls like ls hari ie instead of going into that directory and then doing ls you can directly do it
  ls -l= gives more details of the files like last modified timestamp, file size, permission etc.
  ls -R=gives details about sub directories as well
  ls -lt= gives files in order of latest used
  ls -lr=gives files in order of oldest used
  ls -s= lists files in order of size
  ls *.js= gives all json files in that folder

  ls Zoo*= gives all files which match this regex eg.Zoobear, Zoom Meeting

2.mkdir= you can also create recursively eg. mkdir -p frontend/css, mkdir -p frontend/img etc.
*/