import { createContext } from "react";

// export const CountContext=createContext(0);     //for App4.jsx
//export const CountContext=createContext({count:0,setCount:()=>{}}); //for App5.jsx

export const CountContext=createContext(0);  //for App6.jsx
